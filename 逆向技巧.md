sdk1.getActiveDocument().then(doc => {
    console.log(doc.insertText,'doc===')
})

sdk1.getActiveDocument().then(doc => {
    console.log(doc,'doc===')
})


doc是个空的 insertText有东西
doc.insertText 是个 async编译后的东西 不展示其他堆栈

doc是个proxy  断点打到console.log(doc,'doc===')上
看doc的Handler定义 里面有个get 追溯函数  就看到Proxy的定义了
